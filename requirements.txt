# Hugging Face Spaces runtime deps (top-level only; pip resolves the rest).
#
# Do NOT replace this with a fully-pinned `uv export` and do NOT remove the
# pydantic ceiling below. HF Spaces installs gradio with the `[oauth,mcp]`
# extras, and that `mcp` extra requires `pydantic<=2.12.5`. A full export pins
# `pydantic==2.13.4`, which makes the Space build fail with ResolutionImpossible.
# pydantic-ai-slim needs `pydantic>=2.12`, so 2.12.x is the compatible overlap.
braintrust>=0.24.0
gensim>=4.4.0
gradio>=6.16.0
httpx>=0.28.1
pandas>=3.0.3
pydantic-ai==1.106.0
pydantic-ai-harness[code-mode]>=0.3.0
pydantic-settings>=2.14.1
pydantic<=2.12.5
